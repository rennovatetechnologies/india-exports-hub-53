# Public Vite build-time vars for Railway production.
# Safe to commit: only VITE_* values that ship in the browser bundle.
# Local `npm run dev` still uses `.env.local` (empty API base → Vite /api proxy).

VITE_ALLOW_AUTH_MOCK=false
VITE_API_BASE_URL=https://new-india-exports-production.up.railway.app
VITE_RAZORPAY_KEY_ID=rzp_live_TQt7uXPCezGbMP
